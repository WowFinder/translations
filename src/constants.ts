const targetGenerationDir = 'generated';
const translationsDir = 'translations';
const distDir = 'dist';

const languages = ['en', 'es'];

export { targetGenerationDir, translationsDir, distDir, languages };
